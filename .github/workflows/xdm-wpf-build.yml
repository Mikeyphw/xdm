name: Legacy XDM workflow disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  modern-only:
    runs-on: ubuntu-latest
    steps:
      - run: echo "The WPF workflow is retired. Use XDM Modern CI."
