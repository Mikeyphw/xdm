# XDM Phase 8E direct compile and contract repair

Apply from the XDM repository root with:

```bash
unzip -o "$HOME/Downloads/xdm_phase8e_direct_compile_contract_repair_files.zip" -d "$HOME/Code/xdm"
```

This plain patch contains the Phase 8E Compose weight-import repair, deprecated storage-action cleanup, JUnit 4 test imports, and updated downloader-first architecture contracts. It is intended for a repository at the Phase 8E activity-diagnostics state.
