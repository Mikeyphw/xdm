package com.mikeyphw.xdm.android

/** Exported Add/Download-manager entry point. It must never bypass the durable review boundary. */
class ExternalAddDownloadActivity : ExternalHandoffReviewActivity()
