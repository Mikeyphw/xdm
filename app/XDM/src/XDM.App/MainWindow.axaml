<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        d:DesignWidth="1180"
        d:DesignHeight="760"
        x:Class="XDM.App.MainWindow"
        Title="Xtreme Download Manager"
        Width="1180"
        Height="760"
        MinWidth="900"
        MinHeight="600"
        Background="#0E1116">

  <Window.Styles>
    <Style Selector="Button.primary">
      <Setter Property="Background" Value="#4F8CFF" />
      <Setter Property="Foreground" Value="White" />
      <Setter Property="BorderThickness" Value="0" />
      <Setter Property="Padding" Value="18,10" />
      <Setter Property="CornerRadius" Value="8" />
      <Setter Property="FontWeight" Value="SemiBold" />
    </Style>
    <Style Selector="Button.secondary">
      <Setter Property="Background" Value="#1B212A" />
      <Setter Property="Foreground" Value="#DDE5F2" />
      <Setter Property="BorderThickness" Value="0" />
      <Setter Property="Padding" Value="14,9" />
      <Setter Property="CornerRadius" Value="8" />
    </Style>
    <Style Selector="ListBoxItem">
      <Setter Property="Padding" Value="12,11" />
      <Setter Property="Margin" Value="0,2" />
      <Setter Property="CornerRadius" Value="8" />
    </Style>
    <Style Selector="ListBoxItem:selected">
      <Setter Property="Background" Value="#26354A" />
    </Style>
  </Window.Styles>

  <Grid ColumnDefinitions="228,*">
    <Border Grid.Column="0"
            Background="#12171E"
            BorderBrush="#202833"
            BorderThickness="0,0,1,0"
            Padding="16">
      <Grid RowDefinitions="Auto,*,Auto">
        <StackPanel Spacing="2" Margin="8,8,8,20">
          <TextBlock Text="XDM"
                     FontSize="28"
                     FontWeight="Bold"
                     Foreground="#F3F7FC" />
          <TextBlock Text="Modern core"
                     FontSize="12"
                     Foreground="#7F8B9B" />
        </StackPanel>

        <ListBox Grid.Row="1"
                 ItemsSource="{Binding Sections}"
                 SelectedItem="{Binding SelectedSection}"
                 Background="Transparent"
                 BorderThickness="0">
          <ListBox.ItemTemplate>
            <DataTemplate>
              <Grid ColumnDefinitions="28,*">
                <TextBlock Grid.Column="0"
                           Text="{Binding Glyph}"
                           FontSize="17"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Foreground="#8FB5FF" />
                <TextBlock Grid.Column="1"
                           Text="{Binding Title}"
                           VerticalAlignment="Center"
                           FontWeight="SemiBold"
                           Foreground="#DDE5F2" />
              </Grid>
            </DataTemplate>
          </ListBox.ItemTemplate>
        </ListBox>

        <Border Grid.Row="2"
                Background="#171D25"
                CornerRadius="8"
                Padding="12"
                Margin="0,14,0,0">
          <StackPanel Spacing="4">
            <TextBlock Text="Direct cutover"
                       FontWeight="SemiBold"
                       Foreground="#DDE5F2" />
            <TextBlock Text="Only the .NET 10 Avalonia solution is active."
                       TextWrapping="Wrap"
                       FontSize="11"
                       Foreground="#7F8B9B" />
          </StackPanel>
        </Border>
      </Grid>
    </Border>

    <Grid Grid.Column="1" RowDefinitions="Auto,*,Auto" Margin="30,24,30,20">
      <Grid ColumnDefinitions="*,Auto" Margin="0,0,0,22">
        <StackPanel Spacing="5">
          <TextBlock Text="{Binding CurrentTitle}"
                     FontSize="27"
                     FontWeight="Bold"
                     Foreground="#F3F7FC" />
          <TextBlock Text="{Binding CurrentSummary}"
                     Foreground="#8794A5" />
        </StackPanel>

        <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="10">
          <Button Classes="secondary" Content="Paste URL" IsEnabled="False" />
          <Button Classes="primary" Content="+ Add download" IsEnabled="False" />
        </StackPanel>
      </Grid>

      <Grid Grid.Row="1" RowDefinitions="Auto,*">
        <Grid ColumnDefinitions="*,*,*" ColumnSpacing="12">
          <Border Grid.Column="0" Background="#151B23" CornerRadius="10" Padding="16">
            <StackPanel Spacing="7">
              <TextBlock Text="ACTIVE" FontSize="11" FontWeight="Bold" Foreground="#7F8B9B" />
              <TextBlock Text="{Binding ActiveDownloadCount}" FontSize="26" FontWeight="Bold" Foreground="#F3F7FC" />
              <TextBlock Text="Modern application-state snapshot." FontSize="11" Foreground="#7F8B9B" />
            </StackPanel>
          </Border>
          <Border Grid.Column="1" Background="#151B23" CornerRadius="10" Padding="16">
            <StackPanel Spacing="7">
              <TextBlock Text="SPEED" FontSize="11" FontWeight="Bold" Foreground="#7F8B9B" />
              <TextBlock Text="{Binding AggregateSpeed}" FontSize="26" FontWeight="Bold" Foreground="#F3F7FC" />
              <TextBlock Text="Aggregate throughput from core state." FontSize="11" Foreground="#7F8B9B" />
            </StackPanel>
          </Border>
          <Border Grid.Column="2" Background="#151B23" CornerRadius="10" Padding="16">
            <StackPanel Spacing="7">
              <TextBlock Text="CORE" FontSize="11" FontWeight="Bold" Foreground="#7F8B9B" />
              <TextBlock Text="{Binding CoreStatus}" FontSize="26" FontWeight="Bold" Foreground="#F3F7FC" />
              <TextBlock Text="UI-independent services are loaded." FontSize="11" Foreground="#7F8B9B" />
            </StackPanel>
          </Border>
        </Grid>

        <Border Grid.Row="1"
                Margin="0,14,0,0"
                Background="#12171E"
                CornerRadius="10"
                BorderBrush="#202833"
                BorderThickness="1">
          <StackPanel HorizontalAlignment="Center"
                      VerticalAlignment="Center"
                      Spacing="10"
                      Margin="30">
            <TextBlock Text="◇" FontSize="38" Foreground="#4F8CFF" HorizontalAlignment="Center" />
            <TextBlock Text="Modern core is connected"
                       FontSize="19"
                       FontWeight="SemiBold"
                       Foreground="#E9EEF6"
                       HorizontalAlignment="Center" />
            <TextBlock Text="Category, queue, schedule, download snapshot, application state,&#x0a;and platform services now live outside the Avalonia UI."
                       TextAlignment="Center"
                       TextWrapping="Wrap"
                       Foreground="#7F8B9B"
                       MaxWidth="560" />
            <TextBlock Text="{Binding PlatformDescription}"
                       FontSize="12"
                       Foreground="#8FB5FF"
                       HorizontalAlignment="Center" />
          </StackPanel>
        </Border>
      </Grid>

      <Grid Grid.Row="2" ColumnDefinitions="*,Auto" Margin="0,16,0,0">
        <TextBlock Text="{Binding RuntimeDescription}"
                   FontSize="11"
                   Foreground="#687586" />
        <TextBlock Grid.Column="1"
                   Text="Core port • phase 2"
                   FontSize="11"
                   Foreground="#8FB5FF" />
      </Grid>
    </Grid>
  </Grid>
</Window>
