{
  "capability_ids": [
    "XGO-CAP-HTTP-006"
  ],
  "category": "http",
  "derivation": {
    "behavior": "If-Range/validator-aware resume handles 206/200 semantics safely.",
    "canonical_behavior": "Go resumes only verified ranges and invalidates them when server proves representation changed.",
    "strategy": "merged"
  },
  "donor_ids": [
    "android_native_http",
    "desktop_resume_checkpoint"
  ],
  "expected": {
    "invariants": [
      "Only read-back verified checkpoint blocks are reusable.",
      "A full 200 response after conditional range cannot append to incompatible partial bytes.",
      "Invalid blocks are durably revoked while later valid sparse blocks remain reusable."
    ],
    "restart_representation": true,
    "reuse_old_partial": false
  },
  "fixture_id": "xgo-cap-http-006",
  "input": {
    "resume_from": "verified_sparse_blocks",
    "if_range": "strong validator",
    "cases": [
      "unchanged_206",
      "conditional_200",
      "changed_etag",
      "truncated_staging",
      "sparse_valid_invalid",
      "resume_at_eof",
      "stale_attempt"
    ]
  },
  "schema_version": 1
}
