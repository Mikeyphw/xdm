{
  "capability_ids": [
    "XGO-CAP-OPS-002"
  ],
  "category": "secrets",
  "derivation": {
    "behavior": "Persistent engine state stores secret references instead of ordinary plaintext credentials where possible.",
    "canonical_behavior": "Go resolves SecretRef via platform broker and redacts before persistence/export.",
    "strategy": "redesign"
  },
  "donor_ids": [
    "android_request_security_guard",
    "desktop_download_request"
  ],
  "expected": {
    "invariants": [
      "Go resolves SecretRef via platform broker and redacts before persistence/export."
    ]
  },
  "fixture_id": "xgo-cap-ops-002",
  "input": {
    "representation": "language-neutral behavioral invariant",
    "scenario": "Persistent engine state stores secret references instead of ordinary plaintext credentials where possible."
  },
  "schema_version": 1
}
